name: Github Actions 실행시켜보기

on:
    push:
        branches:
            -main


jobs:
    MY-Deploy-Job:
        runs-on: unbuntu-latest

        steps:
            -name: hello word 출력하기
                run: echo "hello word"